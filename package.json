{
	"name": "@charjkl/ff-browser-std",
	"version": "1.0.0",
	"description": "My Firefox API std lib.",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/firefox-webext-browser": "^111.0.2"
	}
}
